{
  "mcpServers": {
    "airmcp": {
      "command": "npx",
      "args": ["-y", "airmcp"],
      "description": "MCP server for the entire Apple ecosystem — 262 tools across 27 modules. macOS only.",
      "env": {}
    }
  }
}
