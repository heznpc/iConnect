{
  "mcpServers": {
    "airmcp": {
      "command": "npx",
      "args": ["-y", "airmcp"],
      "description": "MCP server for the entire Apple ecosystem — 253 tools across 25 modules. macOS only.",
      "env": {}
    }
  }
}
